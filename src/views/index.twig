{% extends "layouts/app" %}

{% block content %}
    <div class="container">
        <div class="row">
            <div class="col-md-8 col-md-offset-2">



                <h2>Default</h2>
                <p>{{ __("E-Mail Address") }}</p>
                <p>{{ __("Password") }}</p>


                <h2>French</h2>
                <p>{{ __("E-Mail Address", {}, 'default', 'fr') }}</p>
                <p>{{ __("Password",       {}, 'default', 'fr') }}</p>

            </div>
        </div>
    </div>
{% endblock %}
